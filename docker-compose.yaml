services:
  app:
    container_name: "idosets_app"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7010:5000"
    volumes:
      - instance:/main/instance
    restart: always

volumes:
  instance:
    name: "idosets_app_instance"